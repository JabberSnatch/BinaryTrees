Tests possibles : DIT_VS_DREC, SREC_VS_DREC, DREC_VS_DLIST, SREC_VS_SIT
TEST : 1) DIT_VS_DREC
        FloatList : 
    -2 Needed : 
        -f1 : float nbInsert : how many insert inside trees
            Default : 5.000
        -f2 : float randomNode : which node is choosen for spr
            Default : random value between 0 and nbInsert
    -1 Optionnal:
        -f3 : float nbRound : number of round made 
        
        
        BoolList:
    -4 boolean:
        -b1: boolean timeShown : boolean if the time of both test are shown
        -b2: boolean dataCountShown : boolean if dataCount is shown
        -b3: boolean nodeCountShown : boolean if nodeCount is shown
        -b4: boolean randomNodeShown : boolean if the node is shown
        
        
___________________________________________
TEST : 2) SREC_VS_DREC
        FloatList : 
    -2 Needed : 
        -f1 : float nbInsert : how many insert inside trees
            Default : 5.000
        -f2 : float randomNode : which node is choosen for spr
            Default : random value between 0 and nbInsert
    -1 Optionnal:
        -f3 : float nbRound : number of round made 
        
        
        BoolList:
    -4 boolean:
        -b1: boolean timeShown : boolean if the time of both test are shown
        -b2: boolean dataCountShown : boolean if dataCount is shown
        -b3: boolean nodeCountShown : boolean if nodeCount is shown
        -b4: boolean randomNodeShown : boolean if the node is shown
        
        
___________________________________________

TEST : 3) DREC_VS_DLIST
        FloatList : 
    -2 Needed : 
        -f1 : float nbInsert : how many insert inside trees
            Default : 5.000
        -f2 : float randomNode : which node is choosen for spr
            Default : random value between 0 and nbInsert
    -1 Optionnal:
        -f3 : float nbRound : number of round made 
        
        
        BoolList:
    -5 boolean:
        -b1: boolean timeShown : boolean if the time of both test are shown
        -b2: boolean dataCountShown : boolean if dataCount is shown
        -b3: boolean nodeCountShown : boolean if nodeCount is shown
        -b4: boolean randomNodeShown : boolean if the node is shown
        -b5: boolean timeListCreationShown : boolean if the creation of the list is timed.
        ___________________________________________
TEST : 2) SREC_VS_SIT
        FloatList : 
    -2 Needed : 
        -f1 : float nbInsert : how many insert inside trees
            Default : 5.000
        -f2 : float randomNode : which node is choosen for spr
            Default : random value between 0 and nbInsert
    -1 Optionnal:
        -f3 : float nbRound : number of round made 
        
        
        BoolList:
    -4 boolean:
        -b1: boolean timeShown : boolean if the time of both test are shown
        -b2: boolean dataCountShown : boolean if dataCount is shown
        -b3: boolean nodeCountShown : boolean if nodeCount is shown
        -b4: boolean randomNodeShown : boolean if the node is shown
